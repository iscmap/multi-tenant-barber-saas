apiVersion: v1
kind: Service

metadata:
  name: booking-service

spec:
  type: ClusterIP

  selector:
    app: booking-service

  ports:
    - name: http
      protocol: TCP
      port: 8081
      targetPort: 8081