apiVersion: v1
kind: Service

metadata:
  name: availability-service

spec:
  type: ClusterIP

  selector:
    app: availability-service

  ports:
    - name: http
      protocol: TCP
      port: 8082
      targetPort: 8082